name: Archived - GitHub Pages Deploy

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub Pages deployment archived
        run: |
          echo "GitHub Pages deployment is disabled."
          echo "Production deploy target is now Cloudflare Pages."
